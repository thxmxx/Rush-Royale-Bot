# Only has to be run once if not run before
python -m venv .bot_env
# if this does not work add the path to where your python installation is located.
sh .bot_env/bin/activate
pip install -r requirements.txt